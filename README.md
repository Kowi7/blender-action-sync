# blender-action-sync
Blender 5 introduced a new Action Slot system that does not automatically sync the active action across multiple objects. This addon fixes that by automatically syncing the active action of an Armature to all linked Mesh objects — including Shape Keys — whenever it changes.
